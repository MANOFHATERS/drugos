"""v27 root-cause fix verification tests.

Each test corresponds to a specific issue from the v26 forensic audit
and verifies the v27 ROOT-LEVEL fix (not a surface patch).

Test naming convention: test_<issue_id>_<short_description>.py
Issue IDs use the audit's P1-N / P1-ER-N / P2-L-N / P2-B-N / TOP-N / ML-N
format so the audit cross-reference is unambiguous.
"""
